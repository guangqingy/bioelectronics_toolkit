# Runtime dependencies for the bioelectronics_toolkit GUIs and web app.
# tkinter is part of the Python standard library on macOS/Windows and ships
# with the python3 packages on most Linux distros (e.g. `python3-tk`).

flask>=3.0
matplotlib>=3.7
numpy>=1.24
pandas>=2.0
pyabf>=2.3
scipy>=1.10
Pillow>=10.0
pyserial>=3.5
tifffile>=2023.7
